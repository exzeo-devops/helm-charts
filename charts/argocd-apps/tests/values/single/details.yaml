applications:
  test-app:
    namespace: kube-system
    helm:
      repoURL: https://example.github.io/charts
      targetRevision: "x"
      valuesObject:
        replicas: 1
        metrics:
          enabled: true